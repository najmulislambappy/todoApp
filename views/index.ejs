<!DOCTYPE html>
<html lang="en">
<head>
    <style>        	
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box !important;
        }
        .btn {
        background-color: rgb(214, 214, 214);
        border: none;
        color: rgb(238, 87, 87);
        padding: 12px 16px;
        font-size: 16px;
        cursor: pointer;
        }

        /* Darker background on mouse-over */
        .btn:hover {
        background-color: rgb(206, 36, 36);
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, sans-serif !important;
            background: #f4f4f4;
        }
        
        .phone {
            position: relative;
            margin: 4% auto;
            width: 360px;
            height: 660px;
            border: 1px solid #e6e6e6;
            border-radius: 8px;
            overflow: hidden;
            background: #fff;
        }
        
        .header {
            position: relative;
            padding: 25px;
            width: 100%;
            background: linear-gradient(to right, #ff416c, #ff4b2b);
            color: #ffffff;
            line-height: 28px !important;
        }
        
        .removeText {
            font-weight: bolder;
            cursor: pointer;
            padding: 4px 15px;
            display: inline;
            border-radius: 30px;
            text-align: center;
            border: 1px solid #fff;
        }
        
        .header p {
            font-size: 14px;
        }
        
        .todo-body {
            position: relative;
            width: 100%;
            height: 500px;
            overflow-y: scroll;
        }
        
        .todo-body::-webkit-scrollbar {
            background: transparent;
            width: 1px;
        }
        
        .todo-list {
            list-style: none;
        }
        
        .todo-list .todo-item {
            margin-top: 2px;
            padding: 6px;
            border: 0;
            
            border-bottom: 1px solid #e7e7e7;
            color: #4d4d4d;
        }
        
        .todo-item.checked {
            text-decoration: line-through;
        }
        
        input[type="checkbox"] {
            position: relative;
            top: 3px;
            margin-right: 10px;
            width: 17px;
            height: 17px;
        
        }
        
        .todoInput {
            position: absolute;
            bottom: 0;
            left: 0;
            padding: 20px;
            width: 100%;
            border: 0;
            border-top: 1px solid #e7e7e7;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Test Todollist APP</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
    <div class="phone">
        <div class="header">
            <h2>Bappy JS Todo</h2>
            <p>Revisiting the beauty of pure javascript</p>
        </div>

        <div class="todo-body">
            <%for(var i = 0; i < tasks.length; i++) {%>
                <ul class="todo-list">
                    <form action="/delete" method="POST">
                        <div>
                            <input class="todo-item" name="task" value="<%= tasks[i] %>" style="float: left; width: 88%"></input>
                            <button class="btn" style="float: right"><i class="fa fa-close"></i></button>
                        </div>
                    </form>
                </ul>
            <% } %>   
        </div>
        <form action="/save" method="POST">
            <input type="text" name="task" class="todoInput" placeholder="Enter a task and press enter">
        </form>
    </div>
    
</body>
</html>

